import styled from "styled-components";

export const ContentSection = styled.section.attrs(props => ({
    className: 'content-section',
}))`
    width: 100%;
    font-family: 'Open Sans', sans-serif;
    font-size: 0.75rem;
`;